import styled from "@emotion/styled";
import "@fontsource/fredoka-one";

const Wrapper = styled.div`
  font-family: "Fredoka One";
  font-size: 26px;
`;

const Logo = () => <Wrapper>Junior</Wrapper>;

export default Logo;
